lib_component_agent_of_calculation_average_depth_of_subject_domain
=> nrel_main_idtf:
	[Компонент библиотеки. sc-агент вычисления cредней глубины предметных областей]
	(* <- lang_ru;; *);
	[Library component. sc-agent of calculation average depth of subject domain]
	(* <- lang_en;; *);

<- library_of_platform_independent_reusable_components; 
<- library_of_atomic_reusable_components; 

=> nrel_attendant_component: 
	lib_component_ui_menu_file_for_calculation_average_depth_of_subject_domain; 

-> rrel_key_sc_element: 
	.platform_independent_realization_of_sc_agent_of_calculation_average_depth_of_subject_domain; 

<- rrel_key_sc_element: 
	...
	(*
	<- explanation;;
	<= nrel_sc_text_translation:
		...
		(*
		-> rrel_example:
			"file://htmls/Agent_of_calculation_average_depth_of_subject_domain.html"
				(* <- lang_ru;; *);; 
		*);;
	*);;

action_to_calculate_average_depth_of_subject_domain
<- sc_node_not_relation;
=> nrel_main_idtf:
	[действие. вычислить cреднюю глубину предметных областей]
 	(* <- lang_ru;; *);
 	[action. calculate average depth of subject domain]
 	(* <- lang_en;; *);; 
